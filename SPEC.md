# 講義音声ノートAI — 機能仕様書（SPEC）

> **バージョン**: 1.0.0（確定）
> **最終更新**: 2026-05-03
> **ステータス**: ✅ 確定済み
> **参照ドキュメント**: [Gemini API Audio Understanding](https://ai.google.dev/gemini-api/docs/audio)

---

## 1. プロダクト概要

### 1.1 目的
大学の講義音声ファイルをアップロードし、Gemini API を用いて **文字起こし・要約・要点抽出** を行うスマートフォン対応 Web アプリケーション（MVP）。

### 1.2 ターゲットユーザー
- 大学生（講義の復習・ノート作成を効率化したい）
- スマートフォンのブラウザから利用することを主な想定とする

### 1.3 技術構成
| 項目 | 選定 |
|---|---|
| フロントエンド | HTML / Tailwind CSS / JavaScript |
| バックエンド | なし（クライアントサイドのみ） |
| AI モデル | `gemini-2.5-flash`（Gemini API） |
| ホスティング | GitHub Pages（静的サイト） |
| API 通信 | REST API 直接呼び出し（`generativelanguage.googleapis.com`） |

> [!NOTE]
> **モデル確定**: `gemini-2.5-flash` を採用（2026-05-04 更新: `gemini-2.0-flash` は Deprecated のため移行）。

---

## 2. ユーザー導線（User Flow）

```
┌─────────────────────────────────────────────────────────┐
│  ① API キー設定（初回のみ / 設定画面）                    │
│     └─ localStorage に保存                               │
├─────────────────────────────────────────────────────────┤
│  ② 講義名を入力                                          │
│     └─ テキスト入力フィールド（任意・出力ファイル名に使用） │
├─────────────────────────────────────────────────────────┤
│  ③ 音声ファイルを選択                                     │
│     └─ <input type="file" accept="...">                  │
│     └─ ファイル情報表示（名前・サイズ・形式）              │
├─────────────────────────────────────────────────────────┤
│  ④「実行」ボタン押下                                      │
│     └─ バリデーション → API 送信 → プログレス表示          │
├─────────────────────────────────────────────────────────┤
│  ⑤ 結果表示                                              │
│     └─ 3タブ切替: 全文文字起こし / 要約 / 要点リスト       │
│     └─ 各セクションに「コピー」ボタン                     │
│     └─ 「.txt ダウンロード」ボタン（全セクション統合）     │
└─────────────────────────────────────────────────────────┘
```

### 2.1 画面構成

| 画面 | 説明 |
|---|---|
| **メイン画面** | 講義名入力・ファイル選択・実行ボタン・結果表示を 1 ページに統合（SPA） |
| **設定モーダル** | API キーの入力・保存・削除。ヘッダーの歯車アイコンから開く |

### 2.2 各ステップの詳細

#### ステップ①：API キー設定
- 初回アクセス時、API キーが `localStorage` に存在しない場合は設定モーダルを自動表示
- キーは `localStorage.setItem('gemini_api_key', key)` で保存
- ソースコードへの直書きは **厳禁**
- 設定画面から削除（クリア）も可能

#### ステップ②：講義名入力
- `<input type="text">` で講義名を入力（例: 「情報工学概論 第5回」）
- **任意入力**：未入力の場合は日時で代替（例: `ノート_2026-05-03_1730`）
- ダウンロードファイル名に使用される

#### ステップ③：音声ファイル選択
- `<input type="file">` でファイルを選択
- 選択後、以下の情報をUI上に表示：
  - ファイル名
  - ファイルサイズ（人間可読形式: MB 表示）
  - MIME タイプ
- ファイルサイズの上限チェック（後述）

#### ステップ④：実行
- 「実行」ボタン押下で処理開始
- 処理中はボタンを無効化し、プログレスインジケーター（スピナー + ステータスメッセージ）を表示
- 処理ステータス表示例:
  - `「音声ファイルを読み込み中...」`
  - `「Gemini API に送信中...」`
  - `「応答を処理中...」`

#### ステップ⑤：結果表示 & エクスポート
- **タブ切替 UI** で 3 つの出力を表示（詳細は §3 参照）
- 各タブに「📋 コピー」ボタン（`navigator.clipboard.writeText()`）
- 「📥 テキストでダウンロード」ボタン：3 セクションを統合した `.txt` ファイルを生成

---

## 3. 入出力仕様

### 3.1 入力仕様（音声ファイル）

#### 対応フォーマット（公式準拠）

| フォーマット | MIME タイプ | 拡張子 |
|---|---|---|
| WAV | `audio/wav` | `.wav` |
| MP3 | `audio/mp3` | `.mp3` |
| AIFF | `audio/aiff` | `.aiff` |
| AAC | `audio/aac` | `.aac`, `.m4a` |
| OGG Vorbis | `audio/ogg` | `.ogg` |
| FLAC | `audio/flac` | `.flac` |

> **`<input>` の `accept` 属性**:
> ```html
> <input type="file" accept="audio/wav,audio/mp3,audio/mpeg,audio/aiff,audio/aac,audio/ogg,audio/flac,.wav,.mp3,.aiff,.aac,.m4a,.ogg,.flac">
> ```

#### サイズ制限

| 送信方式 | 上限 | 備考 |
|---|---|---|
| **インラインデータ（Base64）** | **20 MB**（リクエスト全体） | フロントエンドのみの構成ではこちらを使用 |
| Files API | 2 GB/ファイル | サーバーサイドが必要なため MVP では対象外 |

> [!WARNING]
> **MVP での制約**: バックエンドを持たない構成のため、Files API は使用できません。音声ファイルを Base64 エンコードしてインラインデータとして送信します。そのため、**ファイルサイズの実質的な上限は約 15 MB**（Base64 エンコードで約 1.33 倍に膨張するため、20MB ÷ 1.33 ≈ 15MB）となります。

#### 音声の技術的仕様（Gemini API 側）
- **トークン変換**: 1 秒あたり 32 トークン（1 分 = 1,920 トークン）
- **最大音声長**: 9.5 時間（単一プロンプト内の合計）
- **ダウンサンプリング**: 16 Kbps にリサンプリングされる
- **マルチチャンネル**: 単一チャンネルに統合される

> **15 MB 制限の場合の目安**:
> - MP3（128kbps）: 約 15 分
> - MP3（64kbps）: 約 30 分
> - OGG（96kbps）: 約 20 分

### 3.2 出力仕様

API に対して **1 回のリクエスト** で 3 種類の出力を一括生成させる。プロンプト内で JSON 形式のレスポンスを指示する。

#### 出力 3 項目の定義

| # | 項目名 | タブ表示名 | 説明 |
|---|---|---|---|
| 1 | **全文文字起こし** | `📝 全文` | 音声の発話内容をそのまま文字に起こしたもの。フィラー（えー、あのー）は適宜除去し、句読点を付与する。 |
| 2 | **要約** | `📋 要約` | 講義内容を **300〜500 字程度** に圧縮した要約文。段落分けして読みやすくする。 |
| 3 | **要点リスト** | `💡 要点` | 講義の重要ポイントを **箇条書き（5〜10 項目）** で抽出。各項目は 1〜2 文で簡潔に。 |

#### API リクエスト設計

**エンドポイント**:
```
POST https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent?key={API_KEY}
```

**リクエストボディ**:
```json
{
  "contents": [
    {
      "parts": [
        {
          "inlineData": {
            "mimeType": "{音声のMIMEタイプ}",
            "data": "{Base64エンコードされた音声データ}"
          }
        },
        {
          "text": "あなたは大学の講義音声を処理する優秀なアシスタントです。以下の音声を分析し、次の3つの項目をJSON形式で出力してください。\n\n1. \"transcript\": 音声の全文文字起こし。フィラー（えー、あのー等）は除去し、適切に句読点を付けてください。\n2. \"summary\": 講義内容の要約（300〜500字程度）。段落分けして読みやすくしてください。\n3. \"keypoints\": 講義の要点を箇条書きリスト（5〜10項目）。各項目は1〜2文で簡潔に記述してください。\n\n出力は以下のJSON形式のみで返してください：\n{\"transcript\": \"...\", \"summary\": \"...\", \"keypoints\": [\"...\", \"...\"]}"
        }
      ]
    }
  ],
  "generationConfig": {
    "responseMimeType": "application/json"
  }
}
```

**レスポンス解析**:
```javascript
const result = JSON.parse(response.candidates[0].content.parts[0].text);
// result.transcript → 全文文字起こし
// result.summary   → 要約
// result.keypoints → 要点リスト（配列）
```

### 3.3 ダウンロードファイル形式

「📥 ダウンロード」ボタンで生成される `.txt` ファイルのフォーマット:

```
========================================
講義音声ノート
========================================
講義名: {講義名 or "未設定"}
作成日時: {YYYY-MM-DD HH:MM}
音声ファイル: {ファイル名}
========================================

【全文文字起こし】
{transcript の内容}

----------------------------------------

【要約】
{summary の内容}

----------------------------------------

【要点】
{keypoints の各項目を箇条書き}
・{keypoint 1}
・{keypoint 2}
・...

========================================
```

ファイル名: `{講義名}_{YYYYMMDD}.txt`（講義名未入力時は `ノート_{YYYYMMDD_HHMM}.txt`）

---

## 4. ストレージ戦略

### 4.1 方針
- **自動保存は行わない**
- 直近 1 件の結果のみ `sessionStorage` に一時保持（タブを閉じると消失）
- ユーザーが明示的に「ダウンロード」操作を行うことで、ローカルに `.txt` ファイルとして保存する

### 4.2 保存対象

| データ | 保存先 | 有効期間 |
|---|---|---|
| API キー | `localStorage` | 明示的に削除するまで永続 |
| 直近の処理結果 | `sessionStorage` | ブラウザタブの存続期間 |
| ノートファイル | ユーザーのローカルストレージ | ユーザー管理 |

---

## 5. エラーハンドリング

### 5.1 バリデーションエラー（実行前）

| 条件 | メッセージ | 動作 |
|---|---|---|
| API キー未設定 | `「APIキーが設定されていません。設定画面からキーを入力してください。」` | 設定モーダルを自動表示 |
| ファイル未選択 | `「音声ファイルを選択してください。」` | ファイル入力欄をハイライト |
| 非対応フォーマット | `「対応していないファイル形式です。WAV, MP3, AAC, OGG, FLAC, AIFF のいずれかを選択してください。」` | — |
| ファイルサイズ超過 | `「ファイルサイズが大きすぎます（上限: 15MB）。音声を圧縮するか、短い音声ファイルをお試しください。」` | — |

### 5.2 API エラー（実行後）

| HTTP Status | 原因 | メッセージ |
|---|---|---|
| `400` | リクエスト不正 | `「リクエストに問題がありました。音声ファイルが破損していないか確認してください。」` |
| `401` / `403` | API キー不正 / 無効 | `「APIキーが無効です。設定画面で正しいキーを再入力してください。」` |
| `429` | レート制限 | `「APIの利用制限に達しました。しばらく時間をおいてから再試行してください。」` |
| `500` / `503` | サーバーエラー | `「サーバーエラーが発生しました。しばらく時間をおいてから再試行してください。」` |
| ネットワークエラー | オフライン等 | `「通信エラーが発生しました。インターネット接続を確認してください。」` |
| レスポンス解析失敗 | JSON パース不可 | `「AIの応答を正しく解析できませんでした。もう一度お試しください。」` |

### 5.3 エラー表示 UI
- エラーメッセージは画面上部に **赤色のアラートバナー** として表示
- 「✕」ボタンで閉じることが可能
- 「再試行」ボタン付き（該当する場合）

---

## 6. API 仕様リファレンス（公式ドキュメント準拠）

> 参照元: https://ai.google.dev/gemini-api/docs/audio（最終更新 2026-04-28）

### 6.1 音声入力方式

本 MVP では **インラインデータ方式** を採用:

```javascript
// ブラウザでのファイル読み込み → Base64 変換
const file = inputElement.files[0];
const reader = new FileReader();
reader.onload = () => {
  const base64Data = reader.result.split(',')[1]; // data:...;base64, 以降
  // API 呼び出しに使用
};
reader.readAsDataURL(file);
```

### 6.2 API 呼び出しフロー

```
[ユーザー] ──ファイル選択──▶ [ブラウザ]
                              │
                        Base64 エンコード
                              │
                        ▼
              fetch() → generativelanguage.googleapis.com
                              │
                        JSON レスポンス
                              │
                        ▼
              パース → UI に表示
```

### 6.3 Files API（将来対応）

大容量ファイル（15MB 超）への対応として、将来的に Files API の利用を検討する。

> [!NOTE]
> Files API はサーバーサイドからの呼び出しが前提であり、フロントエンドのみの構成では CORS 制約により直接利用が困難です。将来のフェーズでバックエンド（Cloudflare Workers 等）を導入する際に対応を検討します。

---

## 7. セキュリティ考慮事項

| 項目 | 対策 |
|---|---|
| API キーの保護 | `localStorage` に保存。ソースコードへのハードコーディング禁禁。 |
| API キーの露出リスク | フロントエンドからの直接呼び出しのため、キーはブラウザの開発者ツールで確認可能。**個人利用を前提** とし、共有端末での利用は非推奨とする注意書きを UI に表示。 |
| 音声データの送信 | HTTPS 通信のみ（`generativelanguage.googleapis.com` は TLS 強制）。 |
| 音声データの保持 | サーバー側（Google）のデータポリシーに準拠。ブラウザ側では処理完了後にメモリ上から Base64 データを破棄。 |

---

## 8. UI / UX 要件

### 8.1 レスポンシブデザイン
- **モバイルファースト**: 幅 320px〜 で正常に表示・操作可能
- ブレイクポイント: `768px`（タブレット）、`1024px`（デスクトップ）

### 8.2 アクセシビリティ
- すべてのインタラクティブ要素に一意の `id` 属性を付与
- `aria-label` / `aria-live` によるスクリーンリーダー対応
- 処理中のステータスは `aria-live="polite"` で通知

### 8.3 デザイン方針
- モダンで清潔感のあるデザイン（ダークモード対応は将来検討）
- アクセントカラー: Google Blue 系
- フォント: システムフォントスタック（日本語対応）

---

## 9. ファイル構成（予定）

```
研修課題6/
├── docs/
│   └── SPEC.md          ← 本ドキュメント
├── index.html           ← メインHTML
├── css/
│   └── style.css        ← スタイルシート
├── js/
│   ├── app.js           ← メインアプリケーションロジック
│   ├── api.js           ← Gemini API 通信モジュール
│   └── ui.js            ← UI 操作・DOM 制御モジュール
└── README.md            ← プロジェクト説明・使い方
```

---

## 10. MVP スコープ外（将来検討）

| 機能 | 理由 |
|---|---|
| Files API 対応 | バックエンド必要 |
| 履歴管理 | ストレージ戦略の方針により除外 |
| リアルタイム文字起こし | Live API が必要（別モデル） |
| 多言語対応 | MVP では日本語に集中 |
| ダークモード | 将来フェーズで検討 |
| PWA 対応 | 将来フェーズで検討 |

---

## 11. 確定事項（2026-05-03 PO承認済み）

> [!NOTE]
> 以下はプロジェクトオーナーにより最終確定されました。

1. **モデル選定**: `gemini-2.5-flash`（またはその時点の最新安定版）を採用。
2. **ファイルサイズ制限**: 15MB 制限を許容。UI 上に「推奨：30分以内（15MB以下）」の注釈を表示し、超過時はバリデーションエラーとする。
3. **API キー導線**: 未設定時は設定モーダルを自動表示。設定方法の簡易ガイドを UI 内に記載。
4. **保存戦略**: 履歴の自動保存は行わない。直近 1 件の一時保持（sessionStorage）と `.txt` ダウンロードに絞る。
